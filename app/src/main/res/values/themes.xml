<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Chrome-free system theme; all real theming happens in Compose (CanonTheme). -->
    <style name="Theme.Canon" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Vellum, so the window behind the first frame matches the page. -->
        <item name="android:windowBackground">@color/canon_vellum</item>
    </style>
</resources>
